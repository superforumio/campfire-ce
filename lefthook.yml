pre-commit:
  parallel: true
  commands:
    rubocop:
      glob: "*.{rb,rake}"
      exclude: "db/schema.rb"
      run: bundle exec rubocop -A {staged_files}
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    brakeman:
      run: bin/brakeman -q --no-pager
