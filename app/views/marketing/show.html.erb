<div class="min-h-screen bg-white">

  <!-- Header Section -->
  <header class="py-6">
    <div class="container" style="display: flex; justify-content: flex-end; gap: 1rem;">
      <a href="<%= new_session_path %>" class="btn btn-outline text-sm py-2 px-6">Sign In</a>
    </div>
  </header>

  <!-- Hero Section -->
  <section class="hero-section">
    <div class="container hero-content">
      <h1 class="hero-title">
        <%= Branding.app_name %>
      </h1>
      <p class="hero-subtitle">
        <%= Branding.app_description %>
      </p>
    </div>
  </section>

</div>
