default: &default
  adapter: redis
  url: redis://localhost:6379

development:
  adapter: any_cable

test:
  adapter: test

performance:
  adapter: <%= ENV.fetch("CABLE_ADAPTER", "redis") %>
  url: redis://localhost:6379
  channel_prefix: campfire_performance

production:
  adapter: any_cable
